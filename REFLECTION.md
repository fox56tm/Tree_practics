
## 1. Публичный интерфейс
В публичный интерфейс множества должны входить только те функции, которые описывают поведение множества как математического объекта,например:
- `void bstInsert(BST* tree, int value)`
- `bool bstContains(BST* tree, int value)`
- `void bstInorder(BST* tree)` (возможно стоит оставить 1 из обходов) 
- `void bstPreorder(BST* tree)`
- `void bstPostorder(BST* tree)`
- `int bstSize(BST* tree)`
- `int bstMin(BST* tree)` 
- `int bstMax(BST* tree)` 
и др.
## Детали реализации
В деталях должны оставаться то, что касается именно структуры BST (например, bstHeight), а также всякие вспомогательные функции: например проверка дерева на валидность(bstValid).

## 2. Функции, не имеющие смысла в интерфейсе множества
Например `bstKthMin` (поиск k-го минимума) не имеет смысла для абстрактного множества. В множестве важен факт наличия элемента, а не его порядковый номер. Также в интерфейсе не должно быть функций, возвращающих указатели на `Node`, так как это раскрывает внутреннее устройство. 

## 3. Замена на другое
- В заголовочном файле можно использовать `typedef struct Set Set;`, а саму структуру описывать только в `.c` файле (Opaque pointer).
- Использовать общие типы данных, не привязанные к деревьям.
